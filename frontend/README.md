# Probis Frontend

React operator terminal for Probis.

## Run

```bash
cd frontend
npm install
npm run dev
```

By default the app targets `http://127.0.0.1:8000`.

Override with:

```bash
cp .env.example .env
```
